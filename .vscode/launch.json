{
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open chrome debugger",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}",
            "sourceMaps": true,
            "trace": "verbose"
        }
    ]
}